
<div class="card-category big" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= cl_image_path @list.photo.key %>)">
<h3><%= @list.name %></h3>
</div>

<div class="container">
  <ul class="bookmark-cards">
    <% @list.bookmarks.each do |bookmark| %>
      <%= render "card", bookmark: bookmark %>
    <% end %>
    <div class="d-flex p-2 bd-highlight justify-content-around mb-5">
      <a class="btn btn-flat d-inline-flex p-2 bd-highlight " href="<% link_to lists_path %>">« Back</a>
      <%= link_to "+ Add a bookmark", new_list_bookmark_path(@list), class: "btn btn-flat-lilac" %>
    </div>
  </ul>
</div>
